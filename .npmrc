# Registry advisory/bulk endpoints (used by `pnpm audit`) can be slow or
# flaky under CI network conditions. Give pnpm more time and more attempts
# before it gives up, on top of its own internal retry/backoff.
fetch-timeout=120000
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
